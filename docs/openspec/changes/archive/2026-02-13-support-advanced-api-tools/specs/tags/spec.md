# Spec: Tags

## ADDED Requirements

### Requirement: List project tags

The system SHALL allow users to list all tags defined in a project.

#### Scenario: List tags successfully

- **WHEN** user requests all tags for project ID "123"
- **THEN** the system SHALL return a list of tag names and metadata
