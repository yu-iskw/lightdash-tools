# Spec: Search

## ADDED Requirements

### Requirement: Global project search

The system SHALL provide a unified search interface to find assets (charts, dashboards, spaces, etc.) within a project.

#### Scenario: Search successfully

- **WHEN** user searches for "revenue" in project ID "123"
- **THEN** the system SHALL return a ranked list of matches across different asset types
