{
  "name": "@lightdash-tools",
  "version": "0.4.0",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "Apache-2.0",
  "author": "yu-iskw",
  "main": "index.js",
  "scripts": {
    "prebuild": "pnpm validate:names",
    "build": "pnpm --recursive build",
    "format": "pnpm format:trunk",
    "format:all": "pnpm format:trunk-all",
    "format:eslint": "eslint . --fix",
    "format:prettier": "prettier --write .",
    "format:trunk": "trunk fmt",
    "format:trunk-all": "trunk fmt --all",
    "lint": "pnpm lint:trunk && pnpm lint:eslint",
    "lint:all": "pnpm lint:trunk-all",
    "lint:eslint": "eslint .",
    "lint:security": "trunk check --all --scope security",
    "lint:trunk": "trunk check",
    "lint:trunk-all": "trunk check --all",
    "openspec": "cd docs && npx @fission-ai/openspec@latest",
    "test": "vitest run",
    "validate:deps": "node scripts/check-common-no-client.mjs",
    "validate:names": "node scripts/validate-package-names.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=24.13.0",
    "pnpm": ">=10.28.1"
  },
  "pnpm": {
    "overrides": {
      "js-yaml": ">=4.1.1",
      "minimatch": ">=5.1.8"
    }
  }
}
